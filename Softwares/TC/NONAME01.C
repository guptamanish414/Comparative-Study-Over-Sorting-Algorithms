#include<stdio.h>
#include<conio.h>
void main()
{char o;
clrscr();
printf("enter the no:\n");
scanf("%c",&o);
if(o)
{printf("the no in between 80 to 100");
}
else("the no in between 60 to 80");
getch();
}

